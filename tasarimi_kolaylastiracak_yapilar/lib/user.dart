import 'package:flutter/material.dart';

class UserSayfasi extends StatelessWidget {
  const UserSayfasi({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.lime.shade300,
      child: Center(
        child: Text("User"),
      ),
    );
  }
}
